
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= set.name %> - Lego Collection</title>
    <link rel="stylesheet" href="/css/main.css">
   
</head>
<header>
    <%- include('partials/navbar', { page: '' }) %> 
</header>
<body>
    <div class="text-center">
    <p class="text-3xl font-bold mb-4">Detailed information about: <%= set.name %></p><br>
    <div class="container mx-auto text-center mt-10">
        <div class="grid grid-cols-1 gap-4 mt-8">
                <div class="card w-96 bg-base-100  shadow-xl mx-auto">
                    <figure><img src="<%= set.img_url %>" alt="Gears" style="max-width: 100%; height: auto"></figure>
                    <div class="card-body">
                        <p class="py-3">Theme: <%= set.theme %></p>
                        <p class="py-3">Year Released: <%= set.year %></p>
                        <p class="py-3">Number of Parts: <%= set.num_parts %></p>
                        <p class="py-3"><div id="quote" class="py-6"></div></p>
                        <a href="#" onclick="history.back(); return false;" class="btn btn-primary">Return</a>
                    </div>
            </div>
        </div>
    </div>
    
    <script>
        document.addEventListener("DOMContentLoaded", () => {
            fetch('https://quotable.io/random')
                .then(response => response.json())
                .then(data => {
                    document.getElementById('quote').innerHTML = `"${data.content}" — ${data.author}`;
                })
                .catch(error => console.log('Error fetching quote:', error));
        });
    </script>
</body>
</html>